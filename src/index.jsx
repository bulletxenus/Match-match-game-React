import React from 'react';
import ReactDOM from 'react-dom';
import './styles.sass';

const root = document.getElementById('root');
const fragment = <span>Hello word</span>;

ReactDOM.render(fragment, root);
